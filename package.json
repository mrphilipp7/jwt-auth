{
  "name": "@mrphilipp7/jwt-auth",
  "version": "1.0.1",
  "main": "./dist/index.js",
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/jsonwebtoken": "^9.0.9"
  },
  "peerDependencies": {
    "express": "^4.18.0",
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "build:types": "tsc -p tsconfig.build.json"
  },
  "type": "module",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  }
}
